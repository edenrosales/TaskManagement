package com.example.taskmanagement;
import java.util.ArrayList;
import java.util.LinkedList;
import java.util.Date;
import java.time.Month;
import java.time.LocalDate;

public class TodoList {
    //Joshua is here and will start to experiment a bit with the todo list class
    LinkedList<Task> todo = new LinkedList<>();

    public void getTaskList(ArrayList<Tag> tags){//we will want to only get the tasks that should be loaded for this day/ interval

    //method will return a list of the necessary tasks that are invoked by a specific filter or a set of filters
    }

    public void displayTaskList(){
    //method will show the tasks obtained from getTaskList() method in a to-do-list fashion
    }

}
